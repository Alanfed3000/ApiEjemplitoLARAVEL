{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan key:generate --show && php artisan serve --host=0.0.0.0 --port=${PORT:8080}"
  }
}